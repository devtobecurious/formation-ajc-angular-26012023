import { MaFirstCharacterPipe } from './ma-first-character.pipe';

describe('MaFirstCharacterPipe', () => {
  it('create an instance', () => {
    const pipe = new MaFirstCharacterPipe();
    expect(pipe).toBeTruthy();
  });
});
